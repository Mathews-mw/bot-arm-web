import { styled } from '@/styles';

export const Container = styled('div', {
	display: 'flex',
	alignItems: 'center',

	marginBottom: '1rem',
});

export const Title = styled('h1', {
	fontWeight: '$medium',
	fontSize: '$2xl',
});
